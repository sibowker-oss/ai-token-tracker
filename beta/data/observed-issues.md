# Observed issues — known but not actioned

Append-only register of issues spotted during routine work that are NOT yet wq-numbered. Each row should be small enough to fix in <0.5 day; larger items get a wq-XXX tracking number instead.

Per GUIDELINES §5.4 — append-only. Don't edit past entries; resolve by adding a "resolved" row referencing the original.

| date | observed-by | area | issue | discovered-during | resolution-notes |
|---|---|---|---|---|---|
| 2026-05-04 | Claude (wq-081 source-collation Phase 1.1) | `site-data.json:entityDirectory` | Duplicate slug in entityDirectory: both `together` and `together-ai` carry `qualifies_for_detail_page=true`, double-counting Together AI in the rendered-entity total (57 → effectively 56 unique). Both list 4–8 fields with `medium` confidence; one of the two is the legacy slug from before the entity registry consolidation. Not in wq-081 scope to fix (would touch entities.json and the renderer). | wq-081 §1 of receipt — computing the 57-entity rendered list. | Untouched in Phase 1.1. Worth a 0.25-day cleanup wq-XXX in the next housekeeping window: pick canonical slug (`together-ai` is more consistent with other slug forms), redirect the legacy slug, ensure `apply_decisions.py` migrates any orphan claims. |
| 2026-05-04 | Claude (wq-081 PR #1 ship-check) | `.github/workflows/release-check.yml` | `release-check.yml` "Post report as PR comment" step reads `tests/reports/html/report.md` but upstream writes elsewhere — workflow infrastructure path mismatch. Causes the post-step to fail with `ENOENT: no such file or directory, open 'tests/reports/html/report.md'` on every PR. Substantive `Run release-check` step succeeds; only the PR-comment posting fails. Separate from wq-081's scope. | wq-081 PR #1 CI investigation. | Fix candidate: align the path in the github-script step with what `scripts/release-check.mjs` actually writes (probably a tests/reports/<timestamp>/ directory rather than tests/reports/html/). No wq number assigned — small enough to fix opportunistically. |
| 2026-05-04 | Claude (wq-083 source-collation Phase 1.2) | `sources-registry.json` cohort coverage | Money Stuff (Matt Levine) Bloomberg archive at `bloomberg.com/account/newsletters/money-stuff` returns HTTP 403 — public archive access blocked, requires email signup or paid Bloomberg subscription. Skipped per Phase 1.2 brief skip-and-log A; no `src-XXX` registered. The capex-as-finance commentary angle remains uncovered in the wq-083 cohort. | wq-083 Tier B source #20 build attempt. | Defer to Phase 2 budget conversation per brief §10a (Stratechery / Platformer / Exponential View / The Diff sit in the same paid-newsletter tier). Email-forward ingestion is theoretically possible but out of scope for Phase 1 free-tier work. |
| 2026-05-04 | Claude (wq-083 source-collation Phase 1.2) | `sources-registry.json` Anthropic + Mistral RSS | Anthropic News (`src-081`) and Mistral AI News (`src-084`) registered without confirmed RSS endpoints — both failed 2 alternate URL patterns each (Anthropic: `/news/feed`, `/rss.xml`; Mistral: `/feed.xml`, `/rss.xml`; all 404). URLs point at canonical news landing pages; `rss_feed` adapter dispatches to `extract_web_page` so HTML scraping works in the interim. | wq-083 Tier A source URL verification. | Manual recheck on next Phase 1.2 sweep — these vendors occasionally expose RSS via 3rd-party feed mirrors (Feedburner, Inoreader public). If still no feed by 2026-06-01, downgrade type/method to `web_page` / `web_extract` per skip-and-log C pattern (already used for DeepMind src-082 and Meta src-083). |
