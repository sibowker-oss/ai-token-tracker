# Deployment: wq-094 — render layer (Claude Code session)

**Date:** 2026-05-07
**WQ:** wq-094
**Branch/Commit:** main / 43d0514
**Author:** Claude Code (VS Code)
**Publishing Gate approval:** Granted by Simon in chat 2026-05-07 ("Excellent - Approved to push to live pages") after staging review at `http://localhost:4173/`. Production publish via push to `main` (GitHub Pages serves from `main` root per `gh api repos/sibowker-oss/ai-token-tracker/pages`).

## What shipped

Render-layer surfacing of the wq-094 data-layer changes that landed in the prior Cowork session (`deploy-2026-05-07-wq-094-zitron-ingest.md`). No production publish — staging-only per CLAUDE.md Publishing Gate.

### Files changed

| File | Change |
|---|---|
| `compute.html` | NEW Forward commitments card (between trajectory chart and Layer-Stack); fetches `data/compute_disclosures.json:forward_commitments` directly. Added `.fc-grid` responsive grid CSS. Added `forwardCommitments` state + parallel fetch in `init()`; added `renderForwardCommitments()`. |
| `capital.html` | NEW Depreciation watch card (after Three-Stage Lag, before Sensitivity); fetches `data/depreciation.json` directly. Added `.tier-pill` styles to capital.html (was using compute.html's). LAG_RAW: 2026E totalCapex 550 → 850; 2027E totalCapex 700 → 1100. |
| `methodology.html` | NEW H2 "Cross-Ledger Reconciliation (wq-094)" with three subsections — (a) forward commitments vs realised compute, (b) hyperscaler→lab investment vs realised capex, (c) Copilot announced-vs-actual three-band table ($7.2B / $2.5–3.5B / $1.35B). |
| `entities.json` | CoreWeave reconciled to filed 10-K + 10-Q values per `dec-2026-05-07-coreweave-reconciliation.md`: 2025.arr/collected_revenue 1.9 → 5.10; +microsoft_concentration_pct 67; +microsoft_backlog_share_at_2025_start_pct 85; +current.backlog_q3_2025_usd_b 55.6; FY2024 collected_revenue/arr added at 1.92 (the value previously mis-attributed as 2025). 2025.arr provenance updated with Tier 1A 10-K claim. |
| `site-data.json` | Regenerated via `python3 scripts/generate_site_data.py` (auto-derived from updated entities). |
| `data/sankey-projections.json` | Auto-regenerated by site-data generator. |
| `docs/decisions/resolved/dec-2026-05-07-coreweave-reconciliation.md` | NEW — full reconciliation document. |

## Verification

- Static server up on `http://localhost:4173/` via `npx http-server -p 4173 -c-1 --silent .`.
- 5 Playwright assertions on `desktop-chrome` AND `mobile-iphone` projects: Forward commitments callouts (RPO, $200B, $718B), Depreciation watch values ($10.1B / $6.5B / $18.9B / $5.9B + WSJ %s), 2026E $850 / 2027E $1100 in SVG text, Cross-Ledger Reconciliation section + Copilot three-band table cells, Anthropic ARR 30.0 / OpenAI 25.0 in `site-data.json`. All 10 passed.
- Provenance validator: 0 fail, 1 advisory (pre-existing, unrelated).
- Screenshots captured to `test-results/wq094-*.png` for visual review.

## Decisions made during implementation

| # | Decision | Resolution |
|---|---|---|
| C1 | Forward commitments card placement | Inline card between trajectory chart and Layer-Stack — keeps the "forward vs realised" pairing on the same scroll. |
| C2 | Forward commitments data source | Fetch `data/compute_disclosures.json` directly (parallel with site-data.json) since `forward_commitments` is not piped into `site-data.json:compute` by `generate_site_data.py`. Lower-effort than extending the generator; acceptable because compute.html already fetches data files. |
| C3 | Capital LAG_RAW out-years (2028E–2030E) | Left at wq-087 trajectory ($750/$750/$700) despite now being lower than the refreshed 2027E $1100. Brief explicitly scoped 2026E + 2027E only; flagging out-year refresh as wq-095 follow-up so the projection stays internally consistent across pages. The chart still reads correctly — accelerating CapEx through 2027 then plateauing — but the 2027→2028 dip is editorially debatable. |
| C4 | Depreciation watch column for Amazon WSJ 2030 | Rendered "—" because depreciation.json's `wsj_2030_projection_pct_net_income` covers Meta/MSFT/GOOGL only (Amazon not in WSJ snippet). Added a defensive lookup `wsj.amzn_2030_pct ?? null`; future depreciation.json refresh can populate it. |
| C5 | CoreWeave reconciliation scope | Applied filed-anchored values to `entities.json:coreweave` only. Did NOT touch `data/compute_disclosures.json:components.coreweave` per-segment math — that requires re-running `derive_compute_revenue.py` and re-validating Layer-Stack tie-outs (Cross-Ledger Reconciliation rule). Documented as wq-095 follow-up. |
| C6 | M365 Copilot product hydration | Did NOT extend `generate_site_data.py` to surface microsoft-copilot product fields into site-data.json. The three-band structure is referenced on `methodology.html` from the Copilot reconciliation table (static copy) and is in `entities.json` for any future page that needs it. Acceptable for this session; wq-095 may want to add a Copilot card on revenue.html that surfaces all three bands live. |

## Open items

### For Simon (this session)

1. **Review staging URL** `http://localhost:4173/` — pages to check:
   - `/compute.html` — Forward commitments card renders with three callouts, source attributions, methodology callout
   - `/capital.html` — Depreciation watch table after Three-Stage Lag; Three-Stage Lag now shows $850 (2026E) and $1100 (2027E)
   - `/methodology.html` — `#cross-ledger-reconciliation` H2 with three subsections + Copilot three-band table
2. **Approve or reject the publish** per Publishing Gate — explicit "approved" / "ship it" / "go live" required before any production push.

### For next Claude Code session (wq-095 candidate scope)

- Refresh `data/compute_disclosures.json:components.coreweave` quarterly trajectory + per-segment split from $4.23B sum-of-Q to filed $5.10B, then re-run `derive_compute_revenue.py` and re-validate Layer-Stack tie-outs and trajectory-chart monotonicity.
- Refresh `LAG_RAW` 2028E–2030E out-years (or convert the chart to read from data/scenarios.json so the editorial trajectory lives in one place).
- Populate `wsj_2030_projection_pct_net_income.amzn_2030_pct` if WSJ data is available, or document why Amazon is excluded.
- Pull CoreWeave Q1 2026 10-Q (filed after today's earnings call) and refresh the trajectory at the next quarterly cycle.
- Decide whether `microsoft-copilot.products[m365-copilot]` three-band fields should be surfaced on a dedicated card on `revenue.html` (currently only methodology.html references the bands).

## Acceptance criteria status

- [x] Build verified — all migrated entity fields hydrate into site-data.json (Anthropic ARR 30, OpenAI ARR 25, M365 Copilot 3-band fields in entities.json, GitHub Copilot tokens_per_day 1.0, OpenAI total_compute_commitment 50)
- [x] Forward commitments block on /compute.html — Google RPO Q4 25→Q1 26, Anthropic-Google $200B/5yr, Anthropic+OpenAI $718B
- [x] Depreciation watch block on /capital.html below time-bomb section — sourced from data/depreciation.json
- [x] /capital.html 2026E refreshed $550B → $850B; 2027E updated to $1.1T
- [x] Methodology Cross-Ledger Reconciliation paragraph — three subsections (forward, investment, Copilot three-band)
- [x] CoreWeave 10-Q reconciled and applied — filed FY2025 $5.10B; Microsoft concentration 67%; full document in `docs/decisions/resolved/dec-2026-05-07-coreweave-reconciliation.md`
- [ ] Staging URL shared + explicit Simon approval — PENDING (this deployment record is the share artefact)

## Notion card status

Notion card needs manual update: `wq-094` In Progress → Done after Simon approves and production publish completes.
